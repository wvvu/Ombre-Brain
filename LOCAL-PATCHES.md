# 本地补丁清单

这个分支（`local/oneplus-deployment`）记录部署在一加 PHP110 / Debian chroot 上的
本地改动。**每条改动必须在这里留下"为什么"**，否则下次升级时无从判断该不该保留。

写这份文档的直接原因：2026-07-29 从 v2.5.0 升级到 v2.10.1 时，本地有两处未提交的
改动只存在于工作区，没有提交信息、没有日期、没有理由，只能靠现场读代码倒推意图。
其中一处早已被上游用另一种方式解决，白白留了三个月。

---

## 现行补丁

### P1 — 提高脱水压缩的触发门槛

- **文件**：`src/dehydrator.py`
- **改动**：`_DEHYDRATE_MIN_TOKENS` 100 → 300
- **首次应用**：2026-07 之前（具体日期不可考，即本文档存在的理由）
- **重新应用**：2026-07-29，升级到 v2.10.1 后

低于该 token 数的内容直接存原文，不调 LLM 压缩。调高到 300 的效果：

1. 省 API 调用 —— 短内容压缩收益本来就低；
2. 更重要的是**防止短内容被压坏**。一两百 token 的东西再"脱水"，
   容易把原本就精炼的表述抹成套话。

**升级时如何处理**：上游默认值仍是 100，每次合并上游都会被覆盖，需要重新应用。
若上游哪天把它变成配置项，改走 `config.yaml`，本补丁即可删除。

---

## 已作废的补丁（保留记录，不要重新应用）

### P2 — 硬删除后手动失效 BM25 缓存 ❌ 已作废

- **文件**：`src/web/buckets.py`（v2.5.0 时期）
- **作废时间**：2026-07-29，升级到 v2.10.1 时确认

原改动在硬删除（hard purge）路径里补了一句 `sh.bucket_mgr._invalidate_bm25()`，
原始注释如下：

```
Hard purge bypasses bucket_mgr.delete(), so we must clear list_all's
in-memory _active_cache (and BM25) or pulse/list will still show ghosts
while trace/get correctly report "not found".
```

症状是：硬删除之后 `pulse` / `list` 里还能看到已删除的桶（鬼影），
但 `trace` / `get` 又正确报告不存在 —— 两边不一致。

**为什么作废**：上游在两个层面上都解决了这个问题，而且比本地补丁更彻底。

1. `_invalidate_bm25()` 已收进 `bucket_manager.py` 内部，在删除路径里自动调用，
   web 层不再需要"记得"手动清缓存 —— 修在了正确的层；
2. 更进一步，`/api/buckets/purge` 端点整个退役了，现在返回 HTTP 410：

   > `physical_deletion_forbidden` —— 记忆会被遗忘，但绝不能被抹去。

   物理删除这条路没了，本补丁针对的成因也就不存在了。

---

## 维护约定

- 新增本地改动 → 在这里加一节，写清文件、改动、**为什么**、以及升级时怎么处理；
- 合并上游后 → 逐条核对本文档，确认每条补丁是仍需重新应用，还是已被上游解决；
- 补丁作废 → 移到"已作废"一节并写明原因，**不要直接删掉**。
  作废记录的价值在于：下次看到类似症状时，能立刻知道这条路已经走过了。

## 关于行尾符（踩过三次）

`.gitattributes` 只对 `*.sh` 强制 LF，`.py` 不在管辖范围内，
而 `src/dehydrator.py` 在上游仓库里就是 CRLF/LF 混合的。由此产生过三次误判：

1. `git diff` 把 1 行改动显示成 1978 行全文件重写 —— 用 `git diff --ignore-cr-at-eol` 才看得到真实改动；
2. 在 Windows 上 clone 会触发 autocrlf 转换，导致文件 sha256 与备份 manifest 全部对不上（实际内容一致）—— clone 时加 `-c core.autocrlf=false`；
3. `sed -i "s/^X = 100$/X = 300/"` **静默不生效** —— 行尾是 `100\r`，`$` 锚点匹配不到。
   这次最阴险，因为它不报错。改动此类文件请用二进制替换，不要依赖行锚点。
